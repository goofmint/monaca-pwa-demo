{
  "name": "monaca-pwa-template-minimum",
  "displayName": "Monaca PWA Template Minimum",
  "dependencies": {
    "gh-pages": "^2.0.1"
  },
  "scripts": {
    "monaca:preview": "npm run dev",
    "dev": "browser-sync start --no-open -s www/ --watch --port 8080 --ui-port 8081",
    "deploy": "gh-pages -d www"
  },
  "devDependencies": {
    "browser-sync": "^2.24.5"
  }
}
